const meetupParamsSchema = require('./meetup-params.validator');
const meetupSchema = require('./meetup.validator');
const userSchema = require('./user.validator');

module.exports = {
  meetupParamsSchema,
  meetupSchema,
  userSchema,
};
